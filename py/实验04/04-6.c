//6.编程实现100元钱买100只鸡问题。现有100元钱，准备买100只鸡，鸡有两种，
//大鸡和小鸡；大鸡3元钱一只，小鸡1元钱3只，钱必须全部花光，问能买几只大鸡和
//小鸡？
#include <stdio.h>

int main(){
    int x,y;
    for (x=0;x<=33;x++){
        y=100-3*x;
        if (y%3==0){
            printf("大鸡%d只，小鸡%d只\n",x,y);
        }
    }
    return 0;

}